# 统一换行符处理，避免 Windows / Linux 协作时出现"整个文件都变了"的假 diff
* text=auto eol=lf

# Windows 批处理脚本必须保留 CRLF，否则会执行异常
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行符转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.jar binary
*.war binary
*.zip binary
