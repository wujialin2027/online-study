# ==================== 敏感配置：绝不提交 ====================
# 真实配置含数据库密码，仓库只提交 application.yml.example
application.yml
application-*.yml
application.yaml
*.env
*.env.*
.env
.env.*

# ==================== Java / Maven ====================
target/
!.mvn/wrapper/maven-wrapper.jar
*.class
*.jar
*.war
*.log
logs/

# ==================== Node / 前端 ====================
node_modules/
dist/
dist-ssr/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ==================== IDE / 编辑器 ====================
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.swp
.obsidian/

# ==================== 操作系统 ====================
.DS_Store
Thumbs.db
desktop.ini

# ==================== 运行时数据 ====================
# 上传目录里的用户附件不入库，但演示用的示例附件（demo-*.png）要入库 ——
# 否则别人 clone 下来跑演示数据脚本后，批改面板的附件预览会是空的。
#
# ⚠️ 两个易错点：
#   ① 必须写成 `**/uploads/*` 而不是 `uploads/*` —— 后者含斜杠，被 Git 视为
#      「相对仓库根目录」的路径，而本项目的 uploads 在 online_study_backend/ 下，
#      会匹配不到（等于没忽略）。
#   ② 也不能写成 `uploads/`（忽略整个目录）—— 父目录被整体忽略时，
#      下面对 demo-*.png 的 `!` 例外规则不会生效。
**/uploads/*
!**/uploads/demo-*.png
.deploy_tmp/

# ==================== 本地工具状态 ====================
.workbuddy/

# ==================== 本地工作材料：绝不进代码仓库 ====================
# _local/ 存放的是"给自己看"的东西：个人求职材料 + 工作过程文档
# （简历、项目说明书、改造日志、前端改造方案、功能对标清单、废弃脚本等）。
# 这些内容不适合出现在开源仓库里，统一放这个目录，整体排除。
_local/
# 已废弃的危险脚本（deploy.ps1 会强杀进程并删除 MySQL 目录），按名字再挡一道
deploy.ps1
# 双保险：即使简历类文件被误放到别处，也不会被提交
简历-*.md
*简历*.md
